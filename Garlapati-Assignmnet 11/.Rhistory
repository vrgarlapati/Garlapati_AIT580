empatt <- read.csv('D:/GMU Student/Github/Garlapati_AIT580-1/data/EmployeeAttrition.csv')
## a.Below is the histogram for the column 'AGE' which displays range from 18 to 60
hist(empatt$Age, main="Histogram of Age", xlab="Age Range",ylab='Count', xlim=c(18,60),breaks=40)+ggsave(histo.png)
ggsave(filename = default_name(), plot = last_plot(),
device = default_device(filename), path = NULL,
scale = 1, width = par("din")[1],
height = par("din")[2], units = c("in", "cm", "mm"),
dpi = 300, ...)
## a.Below is the histogram for the column 'AGE' which displays range from 18 to 60
hist(empatt$Age, main="Histogram of Age", xlab="Age Range",ylab='Count', xlim=c(18,60),breaks=40)
